require "spec_helpers"

describe Scrapespeare::Routing::Rule do

  subject(:rule) { Class.new(Rule) }

  describe "#initialize" do
    it "initializes the given Proc in its instance context" do
      
    end
  end

end
